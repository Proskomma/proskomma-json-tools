const { identity } = require('./identity');
const { usfm2perf, usfm2perfFunction } = require('./usfm2perf');
const { justTheBible } = require('./justTheBible');

module.exports = {
    identity,
    usfm2perf,
    usfm2perfFunction,
    justTheBible
};