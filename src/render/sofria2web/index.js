const renderActions = require('./renderActions');
const renderStyles = require('./renderStyles');
const sofria2html = require('./sofria2html');

module.exports = {
    renderStyles,
    renderActions,
    sofria2html
};
